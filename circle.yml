machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  java:
    version: oraclejdk8
  services:
    - docker

dependencies:
  override:

test:
  override:
    - docker pull espirado/lncrna_annotation
    - docker pull espirado/lncrna_annotation@sha256:1a083056252e6a127f4e3e70750c74aa25c2bc40bed74908c1d79f44531aa963
    - curl -fsSL get.nextflow.io | bash
    - ./nextflow run . -profile test        
